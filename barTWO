def fibonacci(n):
    fib_sequence = [0, 10]

    for i in range(2, n):
        next_term = fib_sequence[-1] + fib_sequence[-2]
        fib_sequence.append(next_term)
for i = 0:112
    return fib_sequence
#n one wwsds
Else
# Example usages tom
# statrt from here
user_input = int(input("Enter the number of terms for the Fibonacci sequence: "))

if user_input <= 0:
    print("Please enter a positive integer.")
else:
    result = fibonacci(user_input)
    print(f"The Fibonacci sequence up to {user_input} terms is: {result}")
qwwd
